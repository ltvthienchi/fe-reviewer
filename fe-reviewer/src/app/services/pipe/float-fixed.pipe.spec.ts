import { FloatFixedPipe } from './float-fixed.pipe';

describe('FloatFixedPipe', () => {
  it('create an instance', () => {
    const pipe = new FloatFixedPipe();
    expect(pipe).toBeTruthy();
  });
});
